import { Layout } from "../../components";
import { Helmet } from "react-helmet";

export const User = () => {
  return (
    <Layout title="Users">
      <Helmet>
        <title>User</title>
      </Helmet>
    </Layout>
  );
};
